# テスト: addf-annotate-grid

## テスト 1: 引数なし — 使い方表示

### 入力
```
/addf-annotate-grid
```

### 期待結果
- 使い方（Usage）が表示される
- `--divide N` と `--every N` の2モードが説明される
- スタイルオプション（--line-color, --label-color 等）が説明される
